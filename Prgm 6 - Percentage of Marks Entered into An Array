#include <stdio.h>
int main() {
    int i,arr[8];
    float avg,sum=0;
    for(i=0;i<8;i++) {
        printf("Enter The Mark %d : ",i+1);
        scanf("%d",&arr[i]);
        sum=sum+arr[i];}
    printf("Percentage of The Marks Entered in the Array %f",(sum/800)*100);
    return 0;}
